# Аудит системи «Цифровий блокнот QC»

**Дата:** 23.08.2026
**Об'єкт:** [yuriybuz.github.io/quality-controller-notebook.](https://yuriybuz.github.io/quality-controller-notebook./) + Apps Script «Бот 1» / «Бот 2» + таблиця **QC Data 2.0**
**Статус:** аудит без внесення змін. Жоден файл додатка, скрипт чи таблиця не змінювалися.

---

## 0. Джерела та межі аудиту

**Що переглянуто:**

| Джерело | Як отримано |
|---|---|
| Код фронтенду (18 файлів) | клон `github.com/YuriyBuz/quality-controller-notebook.`, коміт `b559eb1` |
| Бот 2 «Основний контроль та імпорт» v2.1.0 | вкладення `___2.txt` (554 рядки) |
| Бот 1 «Варки соусу та мийки» | вкладення `___1.txt` (132 рядки) |
| Таблиця QC Data 2.0 | читання через конектор Google Drive, 17 аркушів, ~2 000 рядків |

**Чого переглянути не вдалося — і що це означає:**

1. **`Catalog.js` (Catalog.gs) — вкладений файл порожній (0 байт).** Тому функції `buildCatalog()` і `handleGetCatalog()` не проаналізовано. Усі висновки щодо довідників зроблено з боку клієнта (`qc-core.js`) і за структурою аркушів таблиці. Розділ довідників у цьому аудиті — неповний.
2. **Живий сайт `yuriybuz.github.io` недоступний з середовища аудиту** (заблоковано мережевим проксі). Аналіз зроблено за вихідним кодом репозиторію, який і публікується на GitHub Pages. Якщо на Pages розгорнута інша гілка — висновки треба перепровірити.
3. **Неможливо визначити, яка версія скрипта реально розгорнута за кожним `/exec` URL.** Код Бота 2 у вкладенні — v2.1.0, але чи саме він відповідає за адресою, з якої працюють форми, з боку аудиту не перевіряється. Це саме по собі є проблемою (див. P2-13).

---

# ЧАСТИНА 1. ЯК ЦЕ ПРАЦЮЄ ЗАРАЗ

## 1.1 Загальна архітектура

Система складається з трьох незалежних шарів:

```
   ФРОНТЕНД (GitHub Pages, статичні HTML)
   index.html — меню з 11 плиток
        │
        ├── checklist.html ────────────┐
        ├── QC_Incoming_Control.html ──┤
        ├── QC_Master_Combined.html ───┤   POST (JSON у тілі)
        │     ├─ вкладка «Фасування»   │
        │     └─ вкладка «Етикетування»│
        ├── water_control.html ────────┤
        ├── sauce_boiling.html ────────┤
        ├── cleaning.html ─────────────┤
        ├── Detergents_and_...html ────┤
        ├── QC_Reclamations.html ──────┤
        ├── product_specifications.html│
        └── import_data.html ──────────┘  (читання історії)
                    │
                    ▼
   БЕКЕНД — 6 РІЗНИХ Apps Script деплойментів
        │
        ▼
   ЗБЕРІГАННЯ
   Google Sheets (QC Data 2.0 + таблиця Бота 1) + Google Drive (6 тек) + Gmail
```

## 1.2 Маршрутизація: яка сторінка в який скрипт пише

Це ключова таблиця для розуміння системи. **Використовується шість різних `/exec` адрес.**

| Сторінка | `/exec` (перші символи) | `type` | Куди лягає |
|---|---|---|---|
| `checklist.html` | `AKfycbxG2c79…` | `checklist` | Чек-лист |
| `QC_Master_Combined.html` | `AKfycbxG2c79…` | `packing`, `labeling` | Фасування, Етикетування |
| `water_control.html` | `AKfycbxG2c79…` | `hvo_control` | HVO |
| `import_data.html` | `AKfycbxG2c79…` | `getData` | читання |
| `QC_Incoming_Control.html` | `AKfycbxuLOjr…` | `incoming` | Вхідний контроль |
| `sauce_boiling.html` | `AKfycbxobml8…` | `sauce` | Варки соусу (Бот 1) |
| `cleaning.html` | `AKfycbxobml8…` | `cleaning` | Мийки (Бот 1) |
| `QC_Reclamations.html` | `AKfycbzvbaBw…` | `reclamation` | Рекламації |
| `Detergents_and_disinfectants.html` | `AKfycbw_5c_l…` | — | Миючі засоби |
| `packing.html`, `labeling.html` | `AKfycbwn460T…` | `packing`, `labeling` | **нікуди — файли-сироти** |

**Спостереження:**

* `packing.html` та `labeling.html` **недосяжні з меню** — `index.html` веде обидві плитки на `QC_Master_Combined.html` (рядки 117 і 128 `index.html`, через `localStorage.setItem('qc_initial_tab', …)`). Це старі копії, які ще й дивляться на сьому адресу скрипта.
* Вхідний контроль звертається до **іншого деплойменту**, ніж решта «ботівських двійкових» розділів, хоча пише в ту саму таблицю.
* Рекламації та Миючі засоби — окремі скрипти, повністю поза Ботом 1 / Ботом 2.

## 1.3 Потік запису — два різні механізми

### Механізм А — «сучасний», через `qc-core.js`

**Використовує лише одна сторінка з восьми — `QC_Incoming_Control.html:648`.**

`qc-core.js` (590 рядків) — добре зроблений модуль, який вирішує реальні проблеми цеху:

1. **Відправка без `mode:'no-cors'`.** Не ставиться заголовок `Content-Type` → браузер шле `text/plain` → preflight не потрібен → Apps Script віддає CORS-заголовки → **відповідь сервера читається**. Додаток реально знає, чи записалися дані (`sendRecord`, рядки 154–176).
2. **Черга (outbox) в IndexedDB.** Немає зв'язку → запис лягає в чергу на пристрої, значок унизу зліва показує кількість, автовідправка кожні 60 с і на подію `online` (рядки 68–115, 178–213).
3. **Стиснення фото** до 1600 px / JPEG q0.72, максимум 6 фото (`compressImage`, рядки 296–320).
4. **Ідемпотентний `recordId` (UUID)** у кожному payload (`stamp`, рядок 224) — сервер відсіює повтори.
5. **Розрізнення помилок.** Якщо сервер відповів `{result:'error'}` — помилка позначається `permanent`, запис у чергу не ставиться (повтор не допоможе). Якщо мережевий збій — у чергу (рядки 264–288).
6. **Довідники cache-first** з localStorage, чинні до найближчої 23:00, з деградацією до кешу і далі до вбудованого резервного списку (рядки 400–520).

### Механізм Б — «застарілий», `mode: 'no-cors'`

**Використовують решта сім сторінок:** `checklist.html:621`, `cleaning.html:632`, `sauce_boiling.html:845`, `water_control.html:457`, `QC_Master_Combined.html:1215/1224/1241`, `Detergents_and_disinfectants.html:434/1125`, `QC_Reclamations.html:237`.

```js
await fetch(GOOGLE_SCRIPT_URL, {
    method: 'POST', mode: 'no-cors',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(pendingData)
});
status.innerText = "✅ Дані відправлено!";   // ← безумовно
setTimeout(() => { closeModal(); performReset(); }, 2000);  // ← форма очищається
```

Що тут насправді відбувається:

* `mode:'no-cors'` робить відповідь **opaque**. `fetch` виконується успішно, щойно запит залишив пристрій — **навіть якщо сервер повернув HTTP 500, відхилив токен, не зміг записати в Drive або впав по таймауту**.
* Заголовок `Content-Type: application/json` у режимі `no-cors` **мовчки відкидається браузером** (він не входить у CORS-safelist). Тобто цей рядок не робить нічого.
* `catch` спрацьовує **тільки** на мережевому рівні (немає інтернету, DNS). Будь-яка серверна помилка проходить як успіх.
* Одразу після фальшивого «✅» викликається `performReset()` — форма очищається, введені дані зникають з пристрою.

**Наслідок:** для семи з восьми розділів додаток не має жодного способу дізнатися, чи збереглися дані, і повідомляє про успіх завжди.

## 1.4 Потік читання — `import_data.html` → `handleGetData`

1. Користувач вибирає період, контролера і розділ. За замовчуванням — **сьогодні–сьогодні**.
2. POST `{action:'getData', dateStart, dateEnd, employee, section}`. Тут `no-cors` **не** використовується, тому відповідь читається нормально.
3. Сервер (`___2.txt:332–394`):
   * `historyConfig()` описує 7 розділів: для кожного — ім'я аркуша, індекс колонки дати й контролера, і функція `map(row)`, яка перетворює рядок таблиці на `{section, employee, status, summary, report, photo}`.
   * Для кожного розділу читається **весь аркуш**, далі прохід **знизу вгору** з `break`, щойно дата вийшла за початок періоду.
   * Ліміти: **60 записів на розділ**, **200 усього**.
   * `toDate()` (рядки 214–224) розбирає і справжні `Date`, і текст `dd.MM.yyyy HH:mm:ss` — це виправлення попередньої версії, де сортування працювало навмання.
   * `statusFrom()` (рядки 236–244) дивиться **конкретні** контрольні колонки і шукає `❌`, `🔴`, `Брудно`, `Не в нормі`, `Не відповідає`, `Не працює`. Раніше склеювався весь рядок, тому `❌` у коментарі робив запис невідповідним.
4. Клієнт малює таблицю; «Переглянути» відкриває модалку зі звітом і фото. Посилання Drive конвертуються у `drive.google.com/uc?export=view&id=…`, з фолбеком на кнопку-посилання, якщо картинка не завантажилась.

## 1.5 Фактичний стан даних у QC Data 2.0

17 аркушів. Довідники: `email`, `Персонал` (7 осіб), `Обладнання` (7), `Соуси` (92 позиції з нормами °Bx / Bostwick / pH), сировина+пакування+етикетки (302), продукція/SKU (148), посилання на етикетки (47), `Помилки сканера`.

Аркуші з записами — і **дата останнього запису**:

| Аркуш | Рядків | Останній запис |
|---|---:|---|
| Чек-лист | 355 | 04.05.2026 |
| Мийки | 190 | 09.02.2026 |
| Фасування | 164 | 19.01.2026 |
| Варки соусу | 145 | 17.01.2026 |
| HVO | 145 | 16.06.2026 |
| Вхідний контроль | 136 | 23.01.2026 |
| Етикетування | 98 | 10.01.2026 |
| Рекламації | 44 | 14.07.2026 |

**Це варте окремої уваги.** Сьогодні 23.08.2026, а найсвіжіший запис у всій таблиці — 14.07.2026. Виробничі розділи (фасування, етикетування, вхідний контроль) зупинилися ще в січні. Пояснень може бути два, і їх треба розрізнити перш ніж щось чинити:

* частина розділів пише в **інші таблиці** (інші деплойменти → інші прив'язані документи);
* або записи справді не доходять — що узгоджується з механізмом Б з п. 1.3.

Аркуші **«Варки соусу» і «Мийки» присутні в QC Data 2.0**, хоча за архітектурою їх пише Бот 1 у свою таблицю. Дані в них заморожені на 17.01 і 09.02 — це залишки міграції. Деталі — у проблемі P0-4, там же пастка.

---

# ЧАСТИНА 2. КРИТИЧНА ОЦІНКА

Позначення: **P0** — втрата даних або хибний результат просто зараз; **P1** — приховане спотворення; **P2** — безпека й експлуатація; **P3** — зручність.

## Зведення

| # | Проблема | Пріор. | Наслідок |
|---|---|:--:|---|
| 1 | `no-cors` на 7 з 8 форм: успіх завжди фальшивий | P0 | Втрата запису без сліду |
| 2 | Фото без стиснення на 4 сторінках | P0 | Payload 20–40 МБ → тихий відмов |
| 3 | ХВО: 7 полів не доходять до колонок | P0 | Лабораторні показники втрачені |
| 4 | Історія «Варки соусу» і «Мийки» завжди порожня | P0 | Два розділи невидимі + пастка |
| 5 | `break` ламається на записах із черги | P1 | Історія мовчки обривається |
| 6 | Ліміти 200/60 без попередження | P1 | Неповна вибірка виглядає повною |
| 7 | Немає `recordId` на 7 формах | P1 | Дублікати рядків і листів |
| 8 | Рекламації й Миючі засоби поза історією | P1 | 44+ записи недосяжні |
| 9 | Підпис у «Мийках» стирається при `resize` | P1 | Порожній підпис як доказ |
| 10 | Три різні формати «Дата запису» | P1 | Ненадійна аналітика за період |
| 11 | Хардкод контролерів + точний збіг рядка | P1 | Фільтр губить записи |
| 12 | `/exec` відкритий, токен вимкнено | P2 | Читання і запис будь-ким |
| 13 | Шість деплойментів, версії неконтрольовані | P2 | Виправлення не діють |
| 14 | `packing.html` / `labeling.html` — сироти | P2 | Ризик записів «в нікуди» |
| 15 | Лист кожному окремо | P2 | 2–3× витрати квоти |
| 16 | base64 в `onclick` (чек-лист) | P2 | Зависання браузера |
| 17 | Бот 1 глушить помилки | P2 | Збої невидимі |
| 18 | Дедуп: кеш 6 год, best-effort | P2 | Рідкісні дублі |
| 19 | Читається весь аркуш на кожен запит | P2 | Впреться в ліміт 6 хв |
| 20 | `innerHTML` для даних користувача | P2 | Ламає верстку / інʼєкція |
| 21–27 | Зручність і повнота даних | P3 | див. нижче |

---

## P0-1. `mode:'no-cors'`: «✅ Надіслано» показується завжди

**Де:** `checklist.html:621`, `cleaning.html:632`, `sauce_boiling.html:845`, `water_control.html:457`, `QC_Master_Combined.html:1215/1224/1241`, `Detergents_and_disinfectants.html:434/1125`, `QC_Reclamations.html:237`.

**Суть.** Описано в п. 1.3. Успіх повідомляється безумовно, після чого форма очищається.

**Коли дає хибний результат — реальні сценарії:**

* Сервер відповів `{result:'error', error:'Сервер зайнятий. Спробуйте ще раз.'}` — Бот 2 повертає це, коли `lock.tryLock(30000)` не вдався (`___2.txt:58–60`). Два контролери зберігають одночасно → один запис зникає, обидва бачать «✅».
* `MailApp` вичерпав квоту, `DriveApp` відмовив у доступі до теки, скрипт впав по таймауту виконання — усе це проходить як успіх.
* Payload завеликий (див. P0-2) — Apps Script обриває запит, `fetch` у `no-cors` все одно резолвиться.

**Чому це найважче:** збою не видно **ніде**. Контролер упевнений, що записав. Рядка в таблиці немає. Аркуш `Errors` теж порожній, бо до виконання коду справа не дійшла.

**Підсилювальний фактор:** `performReset()` викликається одразу після фальшивого «✅». Дані стерто з пристрою і не збережено на сервері.

**Виправлення:** перевести всі сторінки на `QC.submit()` з `qc-core.js`. Модуль уже написаний, налагоджений і працює у «Вхідному контролі». Це не розробка з нуля — це підключення готового.

---

## P0-2. Фото без стиснення на чотирьох сторінках

**Де:** `checklist.html:383–402`, `cleaning.html:442–452`, `sauce_boiling.html:670–679`, `water_control.html:300–309`.

```js
const reader = new FileReader();
reader.onload = function(e) {
    const base64 = e.target.result;      // повний розмір, без обробки
    uploadedFiles.push({ name: file.name, type: file.type, data: base64 });
};
reader.readAsDataURL(file);
```

**Суть.** Жодного `canvas`, жодного `drawImage`, жодного `toDataURL` — оригінал файлу як є. Фото з сучасного телефона — 3–8 МБ; base64 додає ще ~33% → **4–11 МБ на знімок**.

Для порівняння: `qc-core.js` стискає до 1600 px / q0.72 (~200–400 КБ), а `QC_Master_Combined.html:1127–1157` і `QC_Reclamations.html:177` — до 1200 px / q0.7. Тобто рішення в проєкті вже є; чотири сторінки просто його не отримали.

**Додатково payload роздувається двічі:** кожна з цих сторінок кладе і `files: uploadedFiles`, і `photo: uploadedFiles[0].data` — тобто перше фото передається **двічі**. При цьому `handlePacking`, `handleLabeling`, `handleIncoming` поле `photo` взагалі не читають — це чистий баласт.

**Наслідок:** 3–4 фото → 20–40 МБ у тілі POST. Apps Script такий запит не приймає. І через P0-1 користувач цього **не бачить**.

**Другий наслідок — пошта.** `sendEmail` чіпляє ті самі блоби вкладеннями. Ліміт вкладень Gmail — 25 МБ; понад це лист не йде взагалі.

**Виправлення:** `QC.gallery()` з `qc-core.js` (стиснення + прев'ю + лічильник «N/6 · ~X МБ» + ліміт кількості). Прибрати дублювання `photo`.

---

## P0-3. ХВО: сім полів заповнюються контролером і не доходять до таблиці

**Це підтверджено фактичними даними в таблиці, а не лише читанням коду.**

`water_control.html:415–435` формує payload з такими іменами:

```js
systemStatus: status,   rotPure: …,   hardness: …,
iron: …,                manganese: …, ph: …,
salt1: salt1,           salt2: salt2, rotSoft: …
```

`handleHVO` (`___2.txt:202`) читає зовсім інші:

```js
data.status, data.salt1, data.rem1, data.salt2, data.rem2, data.aeration,
data.rotSoft, data.rotClean, data.hard, data.fe, data.mn, data.ph
```

**Зіставлення:**

| Колонка аркуша | Читає сервер | Шле клієнт | Результат |
|---|---|---|---|
| Статус системи | `status` | `systemStatus` | **порожньо** |
| Сіль (Бак 1) | `salt1` | `salt1` | ✅ |
| Залишок (Бак 1) | `rem1` | — | **порожньо** |
| Сіль (Бак 2) | `salt2` | `salt2` | ✅ |
| Залишок (Бак 2) | `rem2` | — | **порожньо** |
| Аерація | `aeration` | — | **порожньо** |
| Ротаметр (М'яка) | `rotSoft` | `rotSoft` | ✅ |
| Ротаметр (Чиста) | `rotClean` | `rotPure` | **порожньо** |
| Жорсткість | `hard` | `hardness` | **порожньо** |
| Залізо | `fe` | `iron` | **порожньо** |
| Марганець | `mn` | `manganese` | **порожньо** |
| pH | `ph` | `ph` | ✅ |

**Доказ із таблиці.** Останній запис HVO, 16.06.2026:

```
16.06.2026 10:14:33 | 16.06.2026, 10:14:09 | Попадюк А.М. |  | В нормі |  | В нормі |  |  | 21 |  |  |  |  | 7.00 | 🧪 ЗВІТ QC…
                                              ↑Статус     ↑С1  ↑З1  ↑С2  ↑З2 ↑Аер ↑РМ ↑РЧ ↑Ж ↑Fe ↑Mn ↑pH
```

Заповнені рівно чотири колонки — ті самі, де імена збіглися. Решта порожні в **усіх** 145 рядках.

**Наслідки:**

1. **Жорсткість, залізо, марганець, ротаметр чистої, аерація, залишок солі та статус системи ніде не існують як дані.** Вони є тільки всередині текстового блоба «Текст звіту» і в листі. Побудувати тренд по залізу чи жорсткості неможливо — а для водопідготовки це основний сенс журналу.
2. **Статус в історії завжди «OK».** `statusFrom(row, [3,4,6,8])` (`___2.txt:301`) перевіряє Статус, Сіль 1, Сіль 2, Аерацію. Колонки 3 і 8 порожні завжди. Тобто **проблема з водою ніколи не підсвітиться червоним** в `import_data.html`.
3. **`summary` в історії — «Статус: » без значення** (`___2.txt:302`).

**Виправлення:** привести імена полів до єдиного вигляду. Правильніше змінювати клієнт (`water_control.html`), бо сервер уже узгоджений із порядком колонок аркуша, а колонки чіпати не можна — зламаються старі записи. Одноразово перенести показники зі старих текстових звітів у колонки скриптом.

---

## P0-4. Історія «Варки соусу» і «Мийки» порожня завжди — і пастка при «очевидному» виправленні

**Де:** `___2.txt:31`, `309–328`, `354`.

```js
const BOT1_SPREADSHEET_ID = '';          // рядок 31 — порожньо
…
sauce_boiling: { name: 'Варки соусу', external: true, … },
cleaning:      { name: 'Мийки',       external: true, … },
…
if (conf.external) { if (!BOT1_SPREADSHEET_ID) return; … }   // рядок 354
```

Обидва розділи безумовно пропускаються. У `import_data.html` вони є в списку (`filter-section`, рядки 107 і 110) і завжди дають «Даних не знайдено». Це **335 записів** (145 варок + 190 мийок), недосяжних із додатка.

**Тепер пастка.** Аркуші з такими самими назвами і **точно такою самою структурою колонок** уже лежать у QC Data 2.0. Природний рефлекс — поставити `external: false` і вважати справу закритою. Мапінги при цьому справді збігаються (перевірено поколонково: `colEmp`, індекси `statusFrom`, `summary`, `photo` — усе сходиться).

Але дані в цих аркушах **заморожені**: варки — по 17.01.2026, мийки — по 09.02.2026. Це залишки міграції. Історія почне показувати щось правдоподібне, воно буде відсортоване, з фото і статусами — і буде **застарілим на пів року**, без жодної ознаки цього.

**Виправлення:** спершу з'ясувати, у яку таблицю Бот 1 пише **зараз** (відкрити його проєкт → прив'язаний документ), вписати цей ID у `BOT1_SPREADSHEET_ID`. Мертві копії в QC Data 2.0 — перейменувати в `Варки соусу (архів до 17.01.2026)` та `Мийки (архів до 09.02.2026)`, щоб їх не можна було випадково підхопити. У цільовій схемі (ч. 3) обидва боти пишуть в одну таблицю і поле `external` зникає взагалі.

---

## P1-5. `break` у `handleGetData` ламається на записах з офлайн-черги

**Де:** `___2.txt:377`.

```js
for (let i = values.length - 1; i >= 0; i--) {
    const d = toDate(row[conf.colDate]);
    if (!d) continue;
    if (d.getTime() < start.getTime()) break;   // ← припущення
    …
}
```

`break` виходить із циклу на першому ж рядку старішому за початок періоду. Це коректно **лише якщо порядок додавання збігається з хронологічним**.

Черга `qc-core.js` цю умову порушує за призначенням: запис, зроблений у п'ятницю без зв'язку, відправляється в понеділок і **дописується в кінець аркуша зі старою датою** (`getTimestamp()` ставить момент запису на сервері, але `data.time` — фактичний час; для фільтра береться колонка 0). Достатньо одного такого рядка, щоб прохід зупинився на ньому і **всі записи вище стали невидимими**.

Симптом для користувача: історія за минулий тиждень раптом показує 2–3 записи замість тридцяти. Помилки немає, тост каже «Знайдено записів: 3».

Той самий ефект дає будь-яке ручне сортування аркуша.

**Виправлення:** прибрати `break`, замінити на `continue`; для швидкості обмежити вікно читання (наприклад, останні N рядків або індекс за датою), але не покладатися на порядок.

---

## P1-6. Ліміти 200 / 60 обрізають тихо

**Де:** `___2.txt:334–336, 386, 393`.

`PER_SECTION = 60`, `TOTAL_LIMIT = 200`. У відповіді немає жодної ознаки, що вибірку обрізано. `import_data.html` пише «Знайдено записів: 200» — і це виглядає як повний результат.

За місяць по всіх розділах 200 записів набирається легко (тільки чек-лист дає до 3 на зміну). Тобто **за будь-який період довший за тиждень користувач бачить неповну картину, не знаючи про це**.

Окремо: `PER_SECTION` застосовується **до** глобального сортування, а `TOTAL_LIMIT` — після. Розділ, який «набрав» свої 60 за перші дні періоду, втрачає решту, навіть якщо загальний ліміт не вичерпано.

**Виправлення:** повертати `{items, total, truncated}`; у UI показувати «показано 200 із 837» і додати підвантаження / пагінацію.

---

## P1-7. Дублікати: `recordId` є лише у «Вхідному контролі»

Бот 2 має дедуплікацію (`___2.txt:90–92, 133–140`) — але вона спрацьовує **тільки якщо клієнт надіслав `recordId`**. Його ставить `stamp()` у `qc-core.js:224`, тобто лише на одній сторінці.

Для решти семи форм захисту немає. Найгірший випадок — `QC_Master_Combined.html:1211–1233`, комбінована відправка «Фасування + Етикетування»:

```js
if (storedPackingData) {
    await fetch(…, { body: JSON.stringify(storedPackingData) });   // фасування пішло
    await new Promise(r => setTimeout(r, 1000));
    await fetch(…, { body: JSON.stringify(pendingData) });         // етикетування
    storedPackingData = null;                                       // ← скидання лише тут
}
```

Якщо другий `fetch` впаде на мережевому рівні, `catch` покаже «❌ Помилка з'єднання», форма залишиться заповненою, `storedPackingData` **не обнулиться**. Користувач тисне «Надіслати» ще раз → **фасування записується вдруге**. Плюс другий лист.

`await new Promise(r => setTimeout(r, 1000))` між відправками — спроба обійти конкуренцію за `LockService` затримкою. Це не гарантія, а сподівання.

**Виправлення:** `QC.submit()` ставить `recordId` автоматично. Для комбінованої відправки — або один payload із двома записами, або два незалежні `recordId` з окремим станом «відправлено» для кожного.

---

## P1-8. Рекламації та Миючі засоби взагалі відсутні в історії

`historyConfig()` (`___2.txt:256–330`) описує 7 розділів. Аркуш `Рекламації` (44 записи, найсвіжіший — 14.07.2026, тобто **найактивніший розділ системи**) там не згаданий, як і Миючі засоби. У `filter-section` (`import_data.html:104–113`) їх теж немає.

Обидва розділи є в меню, обидва збирають дані — і жоден не переглядається з додатка. Єдиний доступ — відкрити таблицю вручну.

**Виправлення:** додати `reclamation` і `detergents` у `historyConfig()` та у фільтр. Для рекламацій знадобиться доступ до таблиці їхнього скрипта (окремий деплоймент).

---

## P1-9. Підпис у «Мийках» може зберегтися порожнім

**Де:** `cleaning.html:520–541, 575, 602`.

```js
function resizeCanvas() { canvas.width = canvas.parentElement.offsetWidth; … }
window.addEventListener('resize', resizeCanvas);
```

Присвоєння `canvas.width` **очищає canvas** — це поведінка HTML за специфікацією. Прапорець `hasSignature` при цьому залишається `true`.

На телефоні подія `resize` виникає щоразу, коли з'являється чи ховається екранна клавіатура, і при повороті екрана. Сценарій: контролер підписався → торкнувся поля «Відповідальний», щоб виправити → клавіатура відкрилася → `resize` → малюнок стерто → перевірка `if(!hasSignature)` (рядок 575) проходить → `canvas.toDataURL('image/png')` (рядок 602) віддає **порожній прозорий PNG**.

Порожній файл зберігається в Drive і потрапляє в колонку «Підпис (Посилання)». В таблиці стоїть посилання — виглядає як підтверджений підпис. Для санітарного журналу це дефект доказовості, а не косметика.

**Виправлення:** зберігати вміст (`getImageData`) перед зміною розміру й відновлювати після; перевіряти непорожність не прапорцем, а аналізом пікселів; не реагувати на `resize`, якщо змінилася лише висота (клавіатура).

---

## P1-10. «Дата запису» існує в трьох різних форматах

У колонці 0 зустрічаються щонайменше три конвенції:

| Формат | Приклад | Де |
|---|---|---|
| Повний JS `toString` | `Fri Jan 02 2026 08:54:15 GMT+0200 (за східноєвропейським…)` | перші рядки майже всіх аркушів |
| `dd.MM.yyyy HH:mm:ss` зі зсувом UTC | `04.01.2026 21:14:01` при `Час` = `04.01.2026, 23:13:45` | січень |
| `dd.MM.yyyy HH:mm:ss` локальний | `23.01.2026 18:40:11` при `Час` = `23.01.2026, 18:40:01` | з кінця січня |

Другий рядок — це різниця рівно у 2 години, тобто сервер писав UTC замість київського часу. Пізніші записи (Фасування 19.01, HVO 16.06) вже узгоджені — отже, у якийсь момент це виправили і **зараз запис коректний**. Але **історія залишилася змішаною назавжди**.

`toDate()` (`___2.txt:214–224`) обробляє всі три випадки, тому додаток не падає. Проблема інша: **записи за січень зміщені на 2 години відносно реального часу**, і вечірні з них потрапляють у попередню добу. Будь-яка вибірка «за день» або звіт за зміну по цьому періоду дає хибний результат.

Окремо: колонка «Час» має **різну семантику** в різних аркушах — `Вхідний`/`Фасування`/`Етикетування`/`HVO` пишуть повну дату-час (`toLocaleString('uk-UA')`), `Мийки` і `Варки` — лише `HH:mm`. `reportText` виводить обидва як «Час:», що плутає.

Дрібниця, але варта уваги: Бот 1 використовує `"Europe/Kiev"`, Бот 2 — `'Europe/Kyiv'`. Обидва зараз працюють, але тримати різні написання в системі, де таймзона вже одного разу дала зсув, — зайвий ризик.

**Виправлення:** для нових записів — один формат (найкраще ISO 8601 із зоною). Старі не чіпати, але додати службову колонку з нормалізованим значенням, порахувавши її одноразовим скриптом із поправкою на відомий зсув для січневого періоду.

---

## P1-11. Хардкод контролерів і фільтр за точним збігом

**Де:** `import_data.html:188`.

```js
const employees = ["Соботович Л.М.", "Огаркова М.В.", "Попадюк А.М."];
```

Список зашитий у код, хоча в таблиці є аркуш `Персонал` (7 осіб) і `qc-core.js` уміє віддавати довідник `employees`. Нового контролера треба додавати правкою HTML і комітом.

Причому аркуш `Персонал` теж неповний: у даних «Мийок» фігурує «Єрко Віктор Андрійович» як відповідальний, і в довіднику його немає.

Друге: фільтр на сервері — точний збіг рядка (`___2.txt:379`):

```js
if (filters.employee && filters.employee !== 'all' && row[conf.colEmp] !== filters.employee) continue;
```

Будь-який зайвий пробіл або інше написання («Соботович Л.М.» проти «Соботович Л. М.») → запис не знайдеться. Що в даних такі розбіжності бувають, видно з полів на кшталт `Світ тари ` і `ПП Очім ` — із хвостовими пробілами.

**Виправлення:** брати список із `QC.catalog.get('employees')`; на сервері порівнювати нормалізовано (`String(x).trim()`, згорнуті пробіли).

---

## P2-12. Ендпоінти відкриті, токен вимкнено

`checkToken()` (`___2.txt:124–128`) повертає `true`, якщо властивість `API_TOKEN` не задана. Усі клієнти шлють `token: ''`.

Щоб форми взагалі працювали без входу в Google, деплоймент має стояти в режимі «доступ: Усі». Отже, будь-хто, хто має `/exec` URL, може:

* **прочитати всю історію QC** — `{action:'getData', section:'all', dateStart:'2020-01-01'}` віддасть 200 записів із ПІБ, постачальниками, партіями і фото;
* **записати підроблений запис** у будь-який журнал;
* **розіслати листи** на адреси з аркуша `email` з довільним текстом і вкладеннями.

URL лежать відкритим текстом у публічному репозиторії на GitHub — тобто вони загальнодоступні за визначенням.

Для журналів контролю якості, які є документами системи безпечності харчової продукції, відсутність автентичності записів — суттєвий недолік. Це не «злом», а можливість непомітної підміни.

**Виправлення (мінімум):** задати `API_TOKEN` у властивостях скрипта і в `QC.configure({token:…})`. Це не справжня автентифікація (токен видно в коді сторінки), але відсікає випадкових. Правильніше — Google-авторизація з перевіркою домену або проксі, що підписує запити.

---

## P2-13. Шість деплойментів — виправлення не гарантовано доходять

Система звертається до шести різних `/exec` (п. 1.2). Кожен деплоймент Apps Script прив'язаний до **конкретної версії** коду; оновлення проєкту не оновлює деплоймент, доки не створено нову версію.

Наслідки:
* Виправлення в Боті 2 v2.1.0 (перевірка `tryLock`, аркуш `Errors`, дедуплікація, коректний `toDate`) діють лише там, де перерозгорнуто. З боку аудиту неможливо визначити, де саме.
* Дедуплікація спирається на `CacheService` **окремого проєкту**. Записи через різні деплойменти в різних проєктах між собою не дедуплікуються.
* «Вхідний контроль» ходить в іншу адресу, ніж решта розділів Бота 2, — і саме він єдиний потребує свіжого коду (`getCatalog`).
* Це найімовірніше пояснення розбіжності дат у п. 1.5: частина форм пише в таблиці, яких ніхто не дивиться.

**Виправлення:** звести до двох проєктів (або одного), по одному «prod»-деплойменту на кожен, URL винести в один спільний конфіг-файл фронтенду. `doGet` уже віддає версію (`___2.txt:46–51`) — цим варто регулярно перевіряти, що розгорнуто.

---

## P2-14. `packing.html` і `labeling.html` — мертві файли з чужим URL

Недосяжні з меню, але доступні за прямим посиланням і дивляться на сьому адресу скрипта (`AKfycbwn460T…`). Якщо в когось збережено закладку — записи підуть у невідомий бекенд і не з'являться там, де їх шукають.

**Виправлення:** видалити або залишити заглушку з редиректом на `QC_Master_Combined.html`.

---

## P2-15. Лист надсилається кожному отримувачу окремо

**Де:** `___2.txt:499–506` (те саме в Боті 1, `___1.txt:119–125`).

```js
targetEmails.split(',').forEach(function (email) {
    MailApp.sendEmail({ to: email, subject: …, attachments: … });
});
```

Два отримувачі = **дві одиниці квоти** і два завантаження вкладень; для варок — три (`___1.txt:55`). Квота `MailApp` — 100 листів/добу для звичайного акаунта. При 30 записах на день із двома адресами це 60 одиниць — вже половина ліміту, і при сплеску листи просто перестануть іти.

`getRemainingDailyQuota()` перевіряється **один раз до циклу** (`___2.txt:486`), тому квота може закінчитися посеред розсилки.

**Виправлення:** один `MailApp.sendEmail` із `to` через кому (або `bcc`). Одразу економить у 2–3 рази. Великі фото не чіпляти вкладенням — давати посилання на Drive.

---

## P2-16. Повний base64 фото в HTML-атрибуті

**Де:** `checklist.html:398`.

```js
div.innerHTML = `
    <img src="${base64}">
    <div class="photo-remove-btn" onclick="removeFile(this, '${section}', '${base64}')">✕</div>`;
```

Base64 підставляється в `onclick` **вдруге** — для нестисненого фото це ~7 МБ рядка в DOM-атрибуті на кожен знімок. Саме цей антипатерн описано в коментарі до `qc-core.js:169–170`.

Три фото → десятки мегабайтів у DOM. На телефоні — фриз або вивантаження вкладки браузером разом із незбереженою формою.

Додатково: якщо в base64 трапиться послідовність, що закриває лапку атрибута, розмітка зламається.

**Виправлення:** `QC.gallery()` — тримає дані в JS-масиві, у DOM кладе лише `data-file-id`.

---

## P2-17. Бот 1 глушить помилки; аркуша `Errors` у нього немає

Бот 2 має журнал помилок (`___2.txt:147–158`) — це правильно. Бот 1 не має:

* `lock.tryLock(30000);` (`___1.txt:26`) — **результат ігнорується**, код виконується навіть при таймауті блокування;
* `catch (e) { console.error(e); }` у `createBlobs` (`___1.txt:91`);
* `catch (e) { links.push("Error"); }` у `saveBlobsToDrive` (`___1.txt:101`) — у таблицю пишеться рядок `Error` замість посилання;
* `catch (e) {}` у `sendEmail` (`___1.txt:124`) — **повністю порожній**.

Тобто для «Варок» і «Мийок» (335 записів) збої не фіксуються ніде. Аркуш `Помилки сканера` в таблиці показує, що підхід із журналом уже застосовується в іншій частині системи.

Ще одне: Бот 1 досі містить `JSON.parse(decodeURIComponent(escape(payload)))` (`___1.txt:31`) — трюк, від якого Бот 2 відмовився і сам пояснює чому (`___2.txt:65–68`).

**Окреме спостереження, яке треба перевірити очима.** У вивантаженні таблиці 4-байтові емодзі в текстах звітів виглядають пошкодженими, тоді як 3-байтові — цілі. Розбір байтів у комірці HVO:

```
💧  (U+1F4A7, UTF-8 = F0 9F 92 A7)  →  чотири окремі символи U+00F0 U+009F U+0092 U+00A7   ✗
✅  (U+2705,  UTF-8 = E2 9C 85)     →  один символ U+2705                                   ✓
З   (U+0417)                        →  один символ U+0417                                   ✓
```

Це **не** схоже на суцільну проблему кодування: якби текст десь читався як Latin-1 цілком, то й `✅` розпалося б на три символи, а кирилиця — на два. Розпадаються **тільки** символи поза BMP (ті, що в UTF-16 є сурогатною парою). Така вибірковість — характерна ознака некоректної обробки сурогатних пар, тобто саме того класу трюків, від яких Бот 2 відмовився (`___2.txt:65–68`), а Бот 1 досі містить (`___1.txt:31`).

Ефект видно в **обох** ботів і зокрема в записі HVO від 16.06.2026 — тобто, якщо це реальність таблиці, за адресою Бота 2 розгорнута **не** v2.1.0.

Попри це, **стверджувати не можу**: частину спотворення могла внести конвертація при читанні таблиці інструментом аудиту. Перевірка займає хвилину — відкрити комірку «Текст звіту» останнього запису HVO у самій таблиці й подивитися, чи на місці 💧 і 🧪.

---

## P2-18. Дедуплікація на `CacheService` — не гарантія

`DEDUP_TTL_SEC = 21600` (6 год, `___2.txt:34`). Два обмеження:

1. `CacheService` — **best-effort**: запис може бути витіснений раніше TTL. Тоді повтор пройде як новий.
2. Запис, що пролежав у черзі довше 6 годин (ніч, вихідні — типовий випадок для цеху), після відправки не дедуплікується. Якщо перша спроба насправді дійшла, а відповідь загубилася, — буде дубль.

**Виправлення:** зберігати `recordId` у прихованій колонці аркуша й перевіряти по ній (з кешем як швидким шляхом). Тоді захист безстроковий і переживає перезапуск.

---

## P2-19. Читання всього аркуша на кожен запит

`___2.txt:370`: `sheet.getRange(2, 1, lastRow - 1, sheet.getLastColumn()).getValues()` — для `section:'all'` це **п'ять повних аркушів на кожне натискання «Знайти»**, незалежно від періоду. Зараз це ~900 рядків і працює. При 20–50 тис. рядків запит впреться в ліміт виконання Apps Script (6 хв) і історія перестане відкриватися.

Коментар у коді (рядки 367–369) пояснює, що читання всього аркуша — свідома заміна старому «останні 51 рядок», який мовчки давав порожньо за старіші періоди. Напрямок правильний, але рішення тимчасове.

**Виправлення:** читати вікном із кінця з розширенням, доки не покрито період; або тримати зведений індекс-аркуш; або перейти на BigQuery/іншу БД при подальшому зростанні.

---

## P2-20. `innerHTML` для даних, введених користувачем

**Де:** `import_data.html:265–276` (рядки таблиці), `279–283` (модалка звіту).

```js
tr.innerHTML = `… title="${row.summary}" …`;
document.getElementById('modalContent').innerHTML = formattedText;
```

`summary` і `report` містять довільний текст із поля «Коментар». Лапка в коментарі ламає атрибут `title`; кутова дужка — розмітку. Оскільки система внутрішня, ризик цілеспрямованої атаки низький, але **зіпсоване відображення звіту через звичайний коментар** — цілком реальний сценарій.

**Виправлення:** `textContent` замість `innerHTML`, форматування — через створення вузлів.

---

## P3. Зручність і повнота даних

**21. Категорія зберігається англійським ключем.** У «Вхідному контролі» колонка «Категорія» містить `packaging`, `raw` тощо. У таблиці й у звітах це нечитабельно для людини.

**22. Розрахунковий термін придатності й партія не потрапляють у колонки.** `QC_Master_Combined.html:1075–1076` рахує `calculatedExpiry` і `calculatedBatch` (`L …`) — обидва передаються на сервер і **жодне не записується**: `handleLabeling` (`___2.txt:193`) їх не читає, а в аркуші «Етикетування» немає таких колонок. Значення живуть лише в тексті звіту. Саме звірка розрахованого терміну з фактичним на етикетці — суть цього контролю.

**23. Різна семантика колонки «Час»** — див. P1-10.

**24. Фільтр за замовчуванням «сьогодні–сьогодні»** (`import_data.html:192–194`). Найчастіші запити — «за зміну», «за тиждень», «за місяць». Немає швидких кнопок.

**25. Немає експорту.** Дані з історії не вивантажуються в CSV/Excel і не друкуються (клас `no-print` у розмітці є, повноцінного друкованого вигляду немає). Для журналу, який показують при аудиті, це потрібно.

**26. Поле `photo` дублює перше фото** — див. P0-2. Читають його лише `handleChecklist` і `handleHVO`, і то як запасний варіант.

**27. Довідник `Персонал` розходиться з реальністю** — див. P1-11.

**28. Немає підтвердження виходу з незбереженою формою** ніде, крім `qc-core.js:534` (`beforeunload` під час відправки). Випадкове закриття вкладки з заповненою формою стирає роботу. Чернетка є лише у «Вхідному контролі» (`DRAFT_KEY`, `QC_Incoming_Control.html:653`).

---

# ЧАСТИНА 3. РЕКОМЕНДОВАНА ЦІЛЬОВА ЛОГІКА

## 3.1 Головний принцип

> **Запис вважається збереженим тільки тоді, коли сервер підтвердив це рядком у таблиці. До підтвердження дані не зникають із пристрою.**

Зараз цього принципу дотримується одна сторінка з восьми. Уся решта роботи — його поширення.

Важливо: **проєктувати нічого не треба.** `qc-core.js` — це вже реалізація цільової логіки, перевірена в бою на «Вхідному контролі». Основна задача — не написати нове, а прибрати старе.

## 3.2 Цільовий потік запису

```
Контролер заповнює форму
   │  (чернетка в localStorage кожні N секунд)
   ▼
QC.gallery — фото стискаються одразу (1600px / q0.72), лічильник «3/6 · ~0.9 МБ»
   ▼
QC.submit(payload)
   │  ставить recordId (UUID), token, appVersion, submittedAt
   │  перевіряє розмір; завеликий — каже, скільки прибрати
   ▼
POST без no-cors ──────────────────────────────► doPost
                                                    │ tryLock, інакше чесна помилка
                                                    │ recordId уже був? → success+duplicate
                                                    │ запис у аркуш
                                                    │ фото → Drive
                                                    │ лист одним викликом на всіх
                                                    ▼
   ◄──────────── {result:'success'} ───────────────┘
   │
   ├─ success  → «✅ Записано на сервері» → чернетка стирається → форма скидається
   ├─ error    → «❌ Сервер відхилив: …» → форма НЕ скидається
   └─ мережа   → IndexedDB outbox → значок «⏳ У черзі: 2»
                 автовідправка кожні 60 с і на подію online
                 після 5 невдач → «⛔ не відправлено», показати адміністратору
```

Ключове: **`performReset()` викликається лише в гілці `success`**. Це одна зміна, яка усуває більшу частину втрат даних.

## 3.3 Цільова структура даних

**Правило: кожен показник, який контролер вводить, має власну колонку.** Текстовий звіт — для листа й для читання людиною, не сховище.

Зараз це порушено у ХВО (P0-3) і в етикетуванні (P3-22). Наслідок однаковий: показники не можна ні відфільтрувати, ні звести в тренд, ні порівняти з нормами — хоча норми в таблиці вже є (аркуш «Соуси» містить межі °Bx / Bostwick / pH на кожну позицію).

Це відкриває наступний крок: **автоматична перевірка вимірів проти норм**. Дані для цього вже зібрані, лишається зіставити — і замість ручного «✅/❌» від контролера отримати розрахований висновок із зазначенням, на скільки саме показник вийшов за межу.

**Домовленості про формати:**

| Що | Як | Чому |
|---|---|---|
| `Дата запису` | ISO 8601 з зоною | однозначно, сортується, не залежить від локалі |
| `recordId` | UUID у прихованій колонці | безстрокова дедуплікація |
| Числові показники | число, не текст | інакше немає ні порівняння, ні графіка |
| Довідникові значення | ключ + людська назва | `packaging` → «Пакування» |
| Порожньо | порожня комірка | не `"N/A"`, не `"-"`, не `"Error"` |

## 3.4 Цільова архітектура бекенду

```
                     ОДНА таблиця QC Data 2.0
                     (усі журнали + довідники + Errors)
                              ▲
                              │
         ┌────────────────────┴────────────────────┐
         │   ОДИН проєкт Apps Script               │
         │   ОДИН prod-деплоймент → один /exec     │
         │   doGet повертає версію для перевірки   │
         └────────────────────▲────────────────────┘
                              │
                    один config.js на фронті
                    (URL, токен, версія)
```

Це усуває P0-4 (`external` більше не потрібен), P2-13 (версії) і робить дедуплікацію наскрізною.

Якщо об'єднати проєкти зараз неможливо — мінімум: винести URL у спільний `config.js`, щоб зміна адреси була однією правкою, а не пошуком по восьми файлах.

## 3.5 Цільова історія

* Пагінація з чесним `{items, total, truncated}` — «показано 200 із 837».
* Усі розділи, включно з Рекламаціями та Миючими засобами.
* Швидкі періоди: «Зміна», «Сьогодні», «Тиждень», «Місяць».
* Контролери — з довідника, порівняння нормалізоване.
* Експорт у CSV і придатний до друку вигляд.
* Зведення зверху: скільки записів, скільки невідповідностей, за якими розділами.

---

# ЧАСТИНА 4. ПЛАН ВПРОВАДЖЕННЯ

## Крок 0 — діагностика перед будь-якими змінами (пів дня)

Це треба зробити **першим**, бо від результату залежить обсяг решти.

1. Відкрити кожен із шести `/exec` у браузері. Бот 2 на `doGet` віддає `QC Bot 2 (Master & Import) v2.1.0 | …`. **Записати, що показує кожен.**
2. Для кожного деплойменту подивитися, до якої таблиці він прив'язаний.
3. З'ясувати, куди пише Бот 1 **зараз** → це і є `BOT1_SPREADSHEET_ID`.
4. Відкрити комірку «Текст звіту» останнього запису HVO і подивитися, чи цілі емодзі (див. P2-17).
5. Перевірити аркуш `Errors` — якщо він порожній при тому, що записи не доходять, це підтверджує P0-1 (запити не долітають до коду).
6. **Головне питання, на яке має відповісти цей крок:** чому свіжих записів немає з 14.07.2026 — форми пишуть в іншу таблицю, чи не пишуть узагалі?

## Крок 1 — зупинити втрату даних (1–2 дні)

Порядок від найбільшого ефекту:

1. Перевести на `qc-core.js` (`QC.configure` + `QC.submit` + `QC.gallery`) у такому порядку: `water_control.html` → `cleaning.html` → `sauce_boiling.html` → `checklist.html` → `QC_Master_Combined.html`. Зразок — `QC_Incoming_Control.html`.
2. Разом із цим зникають P0-1, P0-2, P1-7, P2-16 і з'являється офлайн-черга.
3. Виправити імена полів ХВО (P0-3).
4. Прибрати `performReset()` з усіх гілок, крім `success`.

## Крок 2 — повернути видимість (1 день)

5. Вписати `BOT1_SPREADSHEET_ID` (P0-4); мертві копії аркушів перейменувати в архівні.
6. Прибрати `break` у `handleGetData` (P1-5).
7. Додати `{total, truncated}` і показ обрізання (P1-6).
8. Додати Рекламації та Миючі засоби в історію (P1-8).
9. Список контролерів — з довідника; порівняння з `trim()` (P1-11).

## Крок 3 — гігієна (2–3 дні)

10. `API_TOKEN` (P2-12).
11. Один лист на всіх отримувачів (P2-15).
12. Журнал помилок і перевірка `tryLock` у Боті 1 (P2-17).
13. `recordId` у прихованій колонці замість тільки кешу (P2-18).
14. Виправити підпис у «Мийках» (P1-9).
15. Видалити `packing.html` і `labeling.html` (P2-14).
16. `textContent` замість `innerHTML` в `import_data.html` (P2-20).

## Крок 4 — консолідація (за наявності часу)

17. Звести деплойменти, винести URL у `config.js` (P2-13).
18. Нормалізувати дати; додати службову колонку з поправкою на січневий зсув (P1-10).
19. Записувати розрахунковий термін придатності й партію в колонки (P3-22).
20. Автоперевірка вимірів проти норм з аркуша «Соуси» (п. 3.3).
21. Експорт CSV і друкований вигляд (P3-25).
22. Вікно читання замість повного аркуша (P2-19).

---

## Підсумок

Система робить корисну справу і має **правильно спроєктоване ядро** — `qc-core.js` вирішує саме ті проблеми, які виникають у цеху: обрив зв'язку, важкі фото, повторні натискання, недоступний сервер. Модуль написаний акуратно, з поясненнями, що і навіщо виправлено.

Основна біда не в тому, що чогось бракує, а в тому, що **зроблене не розкотили**: сім із восьми форм досі працюють за старою схемою `no-cors`, у якій додаток фізично не може дізнатися, чи збереглися дані, і тому повідомляє про успіх завжди — після чого стирає форму.

Три знахідки підтверджені **фактичними даними в таблиці**, а не лише читанням коду: сім показників ХВО ніколи не потрапляли у свої колонки (145 записів), історія двох розділів недосяжна (335 записів), а «Дата запису» існує в трьох несумісних форматах.

Окремо звертаю увагу на факт із п. 1.5, який виходить за межі коду: **найсвіжіший запис у всій таблиці — 14.07.2026, а виробничі розділи зупинилися в січні**. Або дані йдуть у таблицю, якої ніхто не дивиться, або не йдуть узагалі. Це варто з'ясувати раніше за будь-які виправлення — Крок 0.

Найбільший ефект дає найдешевша дія: підключити вже наявний `qc-core.js` до решти сторінок. Це прибирає більшість проблем P0 і P1 одним заходом.
